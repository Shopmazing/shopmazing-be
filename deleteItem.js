import React, { Component } from 'react'

// it doesn't like the delete name so I changed it to deleteItem
export default class deleteItem extends Component {
    render() {
        return (
            <div>

            </div>
        )
    }
}
